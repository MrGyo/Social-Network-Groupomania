// On déclare une constante DOMAIN pour l'utilsation do localhost:3000 utilisé par le backend
export const DOMAIN = 'http://localhost:3000';
// On déclare une constante dédié au local storage pour la session des utilisateurs
export const LOCAL_STORAGE_USER = 'user';
